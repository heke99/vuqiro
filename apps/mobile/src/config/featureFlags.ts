export const featureFlags = {
  paymentsEnabled: false,
  revenueCatEnabled: false,
  stripeConnectEnabled: false,
  uploadEnabled: false,
  moderationEnabled: true,
  creatorSubscriptionsEnabled: true,
  coinsEnabled: true,
  boostsEnabled: false,
  adminMockMode: true
} as const;
